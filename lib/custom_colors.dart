import 'package:flutter/material.dart';

class CustomColors {

  static const amaranth = Color.fromRGBO(215, 38, 56, 1);
  static const tiffany = Color.fromRGBO(46, 196, 182, 1);

  // main background - dark mode
  static const mainDark = Color.fromRGBO(16, 16, 16, 1);
  // main background - light mode
  static const mainLight = Colors.white;
  // bottom background - dark mode
  static const bottomDark = Color.fromRGBO(31, 31, 31, 1);
  // bottom background - light mode
  static const bottomLight = Color.fromRGBO(227, 227, 227, 1);
  // button color - dark mode
  static const buttonColorDark = Color.fromRGBO(25, 25, 25, 1);
  // button color - light mode
  static const buttonColorLight = Color.fromRGBO(209, 209, 209, 1);


}